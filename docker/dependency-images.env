# Repository-controlled dependency images used by GPU deployments.
# Keep this tag aligned with the WHB amd64 / Python 3.10 / CUDA 11.8 runtime.
FILEX_DOCLING_DEPS_IMAGE=ghcr.io/roamer-remote/filex-docling-deps:py310-cuda118-docling-2.117.0
