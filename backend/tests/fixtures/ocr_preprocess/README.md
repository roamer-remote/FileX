Synthetic PNG used by `test_kb_ocr_preprocess.py` (SC-P1-001).

- `sample.png` — baseline image; with preprocess off and no `KB_OCR_PDF_DPI`, bytes pass through unchanged and OCR render uses DPI 200.
